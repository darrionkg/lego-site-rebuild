export class Lego {
  constructor (public name: string, public description: string, public price: number, public rating: number, public ages: string, public pieces: number, public vipPoints: number) { }
}
